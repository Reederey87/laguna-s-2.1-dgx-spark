# A5 — DFlash depth probe: 8 spec tokens instead of 15. VERDICT: REJECTED.
# Ran at production sampling (--server-defaults): n=8 lost on both classes
# (prose 15.7 vs 20.9 tok/s, code 34.3 vs 43.0) — n=15 kept. See
# docs/PERFORMANCE.md "A5: DFlash depth". Profile kept for reference.
NUM_SPEC_TOKENS=8
MAX_NUM_BATCHED_TOKENS=8192
