# A1 — explicit prefill chunk size. VERDICT: ADOPTED (now the serve.sh default).
# Fixes the GB10 small-GPU heuristic (2048 default -> scheduled 1600 under
# DFlash) to scheduled 7744. Measured: TTFT -23% @8K, -13% @32K, decode
# unchanged, KV pool -5.8%. See docs/PERFORMANCE.md.
MAX_NUM_BATCHED_TOKENS=8192
