# A3 — a third-party recipe's exact serving shape: seqs 4 + batched 8192.
# VERDICT: REJECTED. KV pool SHRANK (815,520 vs 869,932 at a1 — refutes the
# third-party 926K @ seqs-4 claim) and c8 collapses to 37.1 tok/s on queue
# saturation. Card's "max-num-seqs 32 REQUIRED" stands.
# Interaction: spec delta = ~14 x max_num_seqs, so seqs=4 leaves scheduled
# 8136 (vs 7744 at seqs=32).
MAX_NUM_SEQS=4
MAX_NUM_BATCHED_TOKENS=8192
