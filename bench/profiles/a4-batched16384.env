# A4 — 16384 batched tokens. VERDICT: REJECTED. Marginal TTFT gain over a1
# (-0.5% @8K, -4% @32K) for another -9% KV pool (790,825) and a c4 dip
# (33.6 vs 38.2 tok/s). It does start fine — a third-party warning that
# 16384 "can leave no KV blocks" on GB10 did not reproduce.
MAX_NUM_BATCHED_TOKENS=16384
